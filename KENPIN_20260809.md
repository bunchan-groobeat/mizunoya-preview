# KENPIN 2026-08-09 — 水野谷HP 構成比較プレビュー 公開前後検品

対象: https://bunchan-groobeat.github.io/mizunoya-preview/
性質: **検収用プレビュー（本公開ではない）** ／ 社長指示 2026-08-09・承認のうえ太郎が代行実行
根拠: 憲法5条（公開・納品・デプロイの前に `CHECKLIST_KOKAI.md` を流す）

## 判定：🟢 PASS

---

## A. セキュリティ

| 項目 | 結果 |
|---|---|
| APIキー・トークン・鍵・パスワードの混入 | **なし**（grepで実測。ヒットはテーマ切替のlocalStorage処理のみ） |
| .env・秘密設定ファイル | 対象外（静的HTMLのみ） |
| Firestore等のルール | 対象外 |
| フォーム等の脆弱性 | フォームの action は `formspree.io/f/YOUR_FORM_ID` の**プレースホルダ**＝送信先が存在せず、送信は成立しない |
| 顧客情報の混入 | 水野谷様の社名・住所・電話は掲載。**現行公開版で既に公開中の情報と同等**のため新規露出なし |
| **`.bak` 等の混入** | **0件**（追跡ファイルにも無いことを実測）★2026-08の「.bak公開配信」事故の再発防止 |
| **社内メモの混入** | **0件**。［素材待ち］・要撮影・一時非表示ブロックを生成時に自動除去。**配信物5本を実測して漏れなしを確認** |

## B. 動作

| 項目 | 結果 |
|---|---|
| 到達確認 | 入口／onepage／subpages すべて **HTTP 200**（3.1 KB / 17.1 KB / 18.3 KB） |
| HTML構造 | 全7ファイルで `<section>` とHTMLコメントの開閉が一致＝除去による破損なし |
| リンク | 下層構成版の index から5ページへのリンクを設置（ナビ4箇所差し替え＋導線ブロック追加） |
| 画像 | 撮影待ち枠は「写真は準備中です」表示（意図どおり） |

## C. 内容

| 項目 | 結果 |
|---|---|
| 事実の一致 | 中身は既存実装のまま（内容変更なし）。★**比較表の性能値は「当社調べ」表記で実在確認が未了**＝本公開前に水野谷様の確認が必要（既知の申し送り） |
| 「制作中プレビュー」の明示 | **全8ページの画面上部に赤帯**で表示。入口ページにも明記 |

## D. SEO/AIO — ★プレビューなので意図的に「載せない」設定

| 項目 | 結果 |
|---|---|
| noindex | **全8ページに `noindex,nofollow,noarchive`**。配信物で実測確認 |
| robots.txt | `User-agent: * / Disallow: /`（全体拒否）。配信物で実測確認 |
| sitemap | **意図的に置いていない**（検索に載せないため） |
| その他（JSON-LD等） | 本公開時に対応。プレビューでは対象外 |

## E. 記録

- 本ファイル（`KENPIN_20260809.md`）を案件フォルダに保存
- `WORK.md` 4番（Mizunoya）へプレビューURLと構成判断待ちを反映

---

## 🟡 WARN（社長へ申し送り・公開を止めるものではない）

1. **PUBLIC リポジトリである**：GitHub Pages は無料プランでは PRIVATE リポを配信できないため、PUBLIC で作成した。
   検索には載らない（noindex＋robots.txt）が、**URLを知らない人もリポジトリ経由で到達できる**。
   現行公開版が既に PUBLIC で同等の情報を公開中のため実害は小さいと判断したが、
   **構成が確定したらこのリポジトリは削除する**ことを推奨（用済みの公開物を残さない）。
2. **比較表の性能値（耐久年数等）が未確認**：本公開前に水野谷様の確認が必要（既知・本件の判断には影響しない）。

## 実施記録

- 生成スクリプト＝`tools/build-mizunoya-preview.mjs`（dry-run→--commit・社内メモ除去を内蔵）
- 現行公開版リポ（`bunchan-groobeat/mizunoya-hp`）には**一切触っていない**（保全対象・DECISIONS 2026-07-30）
- Pages 設定＝`master` / root・HTTPS強制
